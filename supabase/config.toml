[functions.festival-admin]
# The browser uses a Supabase publishable key in the apikey header.
# The function validates that key itself so both modern publishable keys and
# legacy anon keys work without depending on JWT-shaped keys.
verify_jwt = false
